npm i bcryptjs: cifrar contraseñas 
npm i cors: para las conexiones entre servidores y que no haya conflictos
npm i jsonwebtoken: para crear los toquen y autorizacion
npm i morgan: ver peticiones que se hacen en el servidores
npm i helmet: para generar caracteristicas de seguridad
mkdir controllers libs middlewares models routes

archivo index: cargar la aplicacion de express
archivo app: configurar la aplicacion

Morgan: Morgan es un Middleware de nivel de solicitud HTTP. Es una gran herramienta que registra las requests 
junto con alguna otra información dependiendo de su configuración y el valor predeterminado utilizado.

ref: en schema, sirve para relacionar entre modelo de datos